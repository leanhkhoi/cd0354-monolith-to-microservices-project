version: 2.1

jobs:
  build_and_deploy:
    machine:
        image: ubuntu-2204:2022.04.2
    steps:
      - checkout
      - run:
          name: Login to dockerhub
          command: |
            echo "$DOCKERHUB_PASSWORD" | docker login --username $DOCKERHUB_USERNAME --password-stdin
      - run:
          name: Push udagram-api-feed to Dockerhub
          command: |
            docker build -t udagram-api-feed ./udagram-api-feed
            docker tag udagram-api-feed khoila/udagram-api-feed:v1.0.0
            docker push khoila/udagram-api-feed:v1.0.0
      - run:
          name: Deploy udagram-api-user to Dockerhub
          command: |
            docker build -t udagram-api-user ./udagram-api-user
            docker tag udagram-api-user khoila/udagram-api-user:v1.0.0
            docker push khoila/udagram-api-user:v1.0.0
      - run:
          name: Deploy udagram-frontend to Dockerhub
          command: |
            docker build -t udagram-frontend ./udagram-frontend
            docker tag udagram-frontend khoila/udagram-frontend:v1.0.0
            docker push khoila/udagram-frontend:v1.0.0
      - run:
          name: Deploy udagram-reverseproxy to Dockerhub
          command: |
            docker build -t udagram-reverseproxy ./udagram-reverseproxy
            docker tag udagram-reverseproxy khoila/udagram-reverseproxy:v1.0.0
            docker push khoila/udagram-reverseproxy:v1.0.0

# Define workflows
workflows:
  default:
    jobs:
      - build_and_deploy
